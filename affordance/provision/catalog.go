package provision

const (
	ResourceCPU    = "cpu"    // CPU resource name in .provision grants.
	ResourceMemory = "memory" // Memory resource name in .provision grants.
	ResourceDisk   = "disk"   // Disk resource name in .provision grants.
)
