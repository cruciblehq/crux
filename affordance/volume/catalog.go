package volume

// Access tokens accepted as the optional second positional argument.
const (
	accessRead      = "r"  // Read-only mount (the default when omitted).
	accessReadWrite = "rw" // Read-write mount.
)
